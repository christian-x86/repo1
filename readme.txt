git status (muestra el estado del local)
git checkout (comprueba si ha habido modificaciones en el repo)
git pull (actualiza el local)

1.  git add .
2.  git commit -m "mensaje"git push


Añade un  nombre al  repo y su url
git remote add ejerciciosphp https://github.com/christian-x86/ejerciciosphp.git

Establecer en que brach del repo se va a subir
git push --set-upstream ejerciciosphp main

Eliminar el repo
git remote remove ejerciciosphp

Para terminar eliminar el .git oculto en la  carpeta local para vs code  deje de tratarla como repo